---
import ResetPasswordForm from '../components/resetpass.jsx';

const { email } = Astro.request.url.searchParams;
---

<Layout pubDate={new Date()}>
  <h1>Restablecer Contraseña</h1>
  <ResetPasswordForm client:only="react" email={email} />
</Layout>
